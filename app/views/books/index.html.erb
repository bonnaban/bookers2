<% if @book.errors.any? %>
  <h3><%= @book.errors.count %>errors prohibited this obj from being saved:</h3>
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'list', user: @user, book: @book %>

<%= render 'list2', user: @user, books: @books, book3: @book3 %>

  </div>
 </div>
</main>